# Cline Rules for Class Score Project

## 文档优先原则

在修改任何源代码之前，必须先阅读相关文档。文档目录为 `documents/`，包含以下文件：

- `00_overview.md` - 项目概述和核心概念
- `01_database.md` - 数据库设计和表结构
- `02_models.md` - 数据模型定义
- `03_auth.md` - 认证与授权系统
- `04_student_management.md` - 学生管理系统
- `05_score_system.md` - 评分系统
- `06_backup.md` - 备份与恢复系统
- `07_ui_pages.md` - UI 页面结构

## 文档同步规则

### 修改代码时必须更新文档

当修改以下内容时，必须同步更新对应的文档：

1. **修改数据库表结构**
    - 更新 `01_database.md` 中的表结构说明
    - 更新 `02_models.md` 中的数据模型定义

2. **修改数据模型**
    - 更新 `02_models.md` 中的对应模型说明
    - 更新相关的数据库表结构

3. **修改认证逻辑**
    - 更新 `03_auth.md` 中的相关说明

4. **修改学生管理功能**
    - 更新 `04_student_management.md` 中的相关说明

5. **修改评分系统**
    - 更新 `05_score_system.md` 中的相关说明

6. **修改备份功能**
    - 更新 `06_backup.md` 中的相关说明

7. **修改 UI 页面**
    - 更新 `07_ui_pages.md` 中的页面关系图和说明

### 文档与代码不统一时的处理

如果发现文档和代码不一致或可能产生歧义：

1. 以代码为准
2. 立即更正文档，确保文档反映最新的代码逻辑
3. 在修改记录中说明文档更正的原因

## 遇到问题时的问题解决流程

1. **首先阅读相关文档**，了解该功能的设计意图和实现方式
2. **然后阅读源代码**，验证实现细节
3. **如果发现问题**：
    - 检查文档是否准确描述了代码行为
    - 如果文档有误，更正文档
    - 如果代码有问题，修复代码并更新文档

## 快速参考

| 代码位置                              | 对应文档                   |
| ------------------------------------- | -------------------------- |
| `lib/models/*.dart`                   | `02_models.md`             |
| `lib/database/database_helper.dart`   | `01_database.md`           |
| `lib/providers/auth_provider.dart`    | `03_auth.md`               |
| `lib/providers/student_provider.dart` | `04_student_management.md` |
| `lib/providers/score_provider.dart`   | `05_score_system.md`       |
| `lib/services/backup_service.dart`    | `06_backup.md`             |
| `lib/pages/*`                         | `07_ui_pages.md`           |

## 重要概念提示

- **评分周期 (Period)**: 用于区分不同时间段的评分数据
- **评分目标 (Target)**: 可以是 'student' 或 'group'
- **默认分组**: 名为 "未分组" 的特殊分组
- **USB 密钥**: 存储在 U 盘根目录的隐藏文件 `.class_key` 中
